---
permalink: index.html
---
{% extends "layouts/base.njk" %}

{% set metaTitle = "Inventer le futur" %}
{% set metaImage = site.metaImage %}

{% from "macros/speakerItem.njk" import speakerItem %}
{% from "macros/eventItem.njk" import eventItem %}

{% block content %}

  <div class="c-pagesection">
    <div class="l-container">

      <div class="c-programme">
        <div class="c-programme__media">

          <p class="c-programme__image">
            <img src="/assets/img/programme-400.jpg"
                 srcset="/assets/img/programme-400.jpg 400w,
                         /assets/img/programme-600.jpg 600w,
                         /assets/img/programme-800.jpg 800w"
                 sizes="(min-width: 1024px) 50vw,
                        100vw"
                 class="o-fluidimage"
                 loading="lazy"
                 decoding="async"
                 width="800"
                 height="1066"
                 alt="Flagey au printemps">
          </p>

        </div>
        <div class="c-programme__content">

          <h2 class="c-h2">Programme</h2>
          <p>Cette année, EQUAL fête ses 6 ans d’existence et le départ à la retraite d'Eric Gillet. Deux raisons, au moins, de se réjouir avec vous lors de cet EQUAL DAY 2021.</p>
          <p>Profitons de ce moment convivial et festif pour inventer le futur et papoter autour d’un bon repas.</p>

          {% set events = collections.events %}
          {% for event in events %}
            {% if loop.first %}<ul class="c-vlist">{% endif %}
              <li>
                {{ eventItem(
                  title = event.data.title,
                  startDate = event.data.startDate,
                  endDate = event.data.endDate,
                  content = event.templateContent | safe
                ) }}
              </li>
            {% if loop.last %}</ul>{% endif %}
          {% endfor %}
        </div>
      </div>

    </div>
  </div>
  <div class="c-pagesection">
    <div class="l-container">

      <h2 class="u-hide">Intervenants</h2>

      {% set speakers = collections.speakers %}
      {% for speaker in speakers %}
        {% if loop.first %}<ul class="l-grid  l-grid--gap  l-grid--2cols@small  l-grid--3cols@medium  l-grid--4cols@large">{% endif %}
          <li>
            {{ speakerItem(
              url = speaker.data.url,
              name = speaker.data.name,
              surname = speaker.data.surname,
              function = speaker.data.function,
              image = speaker.data.image
            ) }}
          </li>
        {% if loop.last %}</ul>{% endif %}
      {% endfor %}

    </div>
  </div>
{% endblock %}
