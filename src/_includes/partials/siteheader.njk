<header class="c-siteheader">
  <div class="l-container  l-container--medium">
    <p class="c-siteheader__logo">{% include "svg/logo.svg" %}</p>

    <div class="c-siteheader__info">
      <div class="c-headerdata">
        <p class="c-headerdata__date"><time datetime="{{ event.date | date('yyyy-LL-dd') }}">{{ event.date | date('DDD', 'fr') }}</time></p>
        <h1 class="c-headerdata__title">{{ event.title }}</h1>
        <p class="c-headerdata__location">{{ event.location }}</p>
      </div>
    </div>

    <div class="c-siteheader__quote">
      <div class="c-headerquote">
        <p>{{ event.quote }}</p>
      </div>
    </div>

    <div class="c-siteheader__booking">
      <div class="c-booking">
        <p class="c-booking__cta">
          <span class="c-booking__intro">Places limitées, réservez sans tarder</span>
          <a class="c-booking__link" href="{{ event.bookingUrl }}">Réserver</a>
        </p>
        <div class="c-booking__legal  u-text-s">
          <p>les mesures COVID nous contraignent à demander aux participants de présenter un <a href="https://www.masante.belgique.be">COVID Safe Ticket</a> à l'entrée. Les <a href="https://www.flagey.be/fr/page/7708-nos-mesures-de-securite-covid-19">mesures de sécurité COVID</a> mises en place à Flagey.</p>
        </div>
      </div>
    </div>

  </div>
</header>